server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          #uri: file:///D:/GitLab/config-repo #from local repo
          uri: https://github.com/vibingithub/config-repo
